data: "/path/to/X.pt"                 # torch tensor (N, T, F)
target: "/path/to/y.pt"               # torch tensor (N,)
optim_size: 1000                      # subset used for BO
train_size: "50,100,200"              # comma-separated train sizes for CV
n_resamples: 5
n_trials: 100
seed: 93

# feature selection / preprocessing
feature_k: 2000
var_thresh: 1e-5
fs_sample: 1000     # sample size for MI selection (if using subset)

# IO
res_dir: "./results/"
tmp_dir: "./tmp/"
suffix: "elnet_experiment"
best_params: null   # path to best_params.pkl to skip BO (optional)